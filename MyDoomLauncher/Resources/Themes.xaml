<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Dark Theme Colors -->
    <SolidColorBrush x:Key="DarkBackground" Color="#1e1e1e"/>
    <SolidColorBrush x:Key="DarkBackgroundSecondary" Color="#2d2d2d"/>
    <SolidColorBrush x:Key="DarkForeground" Color="#ffffff"/>
    <SolidColorBrush x:Key="DarkBorder" Color="#3f3f3f"/>
    <SolidColorBrush x:Key="DarkButtonBackground" Color="#2d2d2d"/>
    <SolidColorBrush x:Key="DarkButtonHover" Color="#3d3d3d"/>
    <SolidColorBrush x:Key="DarkGridViewSeparator" Color="#1a1a1a"/>

    <!-- Light Theme Colors -->
    <SolidColorBrush x:Key="LightBackground" Color="#ffffff"/>
    <SolidColorBrush x:Key="LightBackgroundSecondary" Color="#f5f5f5"/>
    <SolidColorBrush x:Key="LightForeground" Color="#000000"/>
    <SolidColorBrush x:Key="LightBorder" Color="#cccccc"/>
    <SolidColorBrush x:Key="LightButtonBackground" Color="#e1e1e1"/>
    <SolidColorBrush x:Key="LightButtonHover" Color="#d1d1d1"/>
    <SolidColorBrush x:Key="LightGridViewSeparator" Color="#666666"/>

    <!-- Active Theme Colors (will be set at runtime) -->
    <SolidColorBrush x:Key="WindowBackground" Color="#1e1e1e"/>
    <SolidColorBrush x:Key="WindowForeground" Color="#ffffff"/>
    <SolidColorBrush x:Key="ControlBackground" Color="#2d2d2d"/>
    <SolidColorBrush x:Key="ControlForeground" Color="#ffffff"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#3f3f3f"/>
    <SolidColorBrush x:Key="ButtonBackground" Color="#2d2d2d"/>
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="#3d3d3d"/>
    <SolidColorBrush x:Key="GridViewSeparator" Color="#1a1a1a"/>

    <!-- Global Styles -->
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{DynamicResource WindowBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource WindowForeground}"/>
    </Style>

    <Style TargetType="{x:Type Control}">
        <Setter Property="Foreground" Value="{DynamicResource ControlForeground}"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource ControlForeground}"/>
    </Style>

    <Style TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{DynamicResource ControlForeground}"/>
    </Style>

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource ControlForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="Padding" Value="5"/>
    </Style>

    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource ControlForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="Padding" Value="10,5"/>
    </Style>

    <Style TargetType="{x:Type ListViewItem}">
        <Setter Property="Foreground" Value="{DynamicResource ControlForeground}"/>
        <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
    </Style>

    <Style TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource ControlForeground}"/>
    </Style>

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{DynamicResource ControlForeground}"/>
    </Style>

    <!-- Scrollbar Styles for dark mode -->
    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="Width" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid x:Name="GridRoot" Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <RepeatButton Grid.Row="0" Command="ScrollBar.LineUpCommand" Background="{TemplateBinding Background}" Foreground="{TemplateBinding Foreground}" Height="16">
                            <Path Data="M 0 4 L 8 4 L 4 0 Z" Fill="{TemplateBinding Foreground}" Margin="0,2,0,2"/>
                        </RepeatButton>
                        <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="false">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Background="Transparent"/>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Background="{DynamicResource BorderBrush}" Foreground="{TemplateBinding Foreground}" Style="{DynamicResource ScrollBarThumb}"/>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Background="Transparent"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                        <RepeatButton Grid.Row="2" Command="ScrollBar.LineDownCommand" Background="{TemplateBinding Background}" Foreground="{TemplateBinding Foreground}" Height="16">
                            <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="{TemplateBinding Foreground}" Margin="0,2,0,2"/>
                        </RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border CornerRadius="2" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding Foreground}" BorderThickness="0"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
